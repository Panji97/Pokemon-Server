import MySQLProvider from '../providers/mysql'

/**
 * --------------------------------------------------------------------------
 * Database Connection
 * --------------------------------------------------------------------------
 * For configuration issue just see the file imported
 */
export const mysqlClient = new MySQLProvider()
